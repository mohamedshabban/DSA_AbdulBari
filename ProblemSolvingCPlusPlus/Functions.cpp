#include <stdio.h>
#include <stdlib.h>
#include <iostream>
using namespace std;

class Calculations {
public:
	/*
	 Calculations calc;
    int x = 10, y = 15;
    cout << "Total = " << calc.Add(x, y) << endl;
	*/
	int Add(int x, int y) {	
		return x + y;
	}
};